#include "UserInterface.h"

int main(){
  UserInterface interface;

  interface.getInput();
   /*   cout << graph.getTitle() << "\n";
      node = graph.getStartNode().first;
      cout << ((StartStopNode *)node)->getActor() << "\n";
      cout << ((StartStopNode *)node)->getMessage() << "\n";

      node = graph.getNextNode().first;
      while(node->getTraverseType() != STOP){
        cout << ((Task *)node)->getActor() << "\n";
        cout << ((Task *)node)->getTask() << "\n";
        */
	return 0;
}
